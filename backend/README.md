# SecureEval backend

This directory is reserved for the FastAPI application introduced in Phase 1.
Phase 0 intentionally contains no executable backend, persistence layer, job
worker, scanner, sandbox, or model-provider integration.

The governing boundaries are defined in the repository-root
`ARCHITECTURE.md`, `API_SPEC.md`, `SECURITY_DESIGN.md`, and
`LLM_OUTPUT_CONTRACTS.md` documents.
